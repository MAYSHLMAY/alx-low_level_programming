#include "stdio.h"


int main(void)
{
	int first = 0;

	int second = 1;

	int sum, temp;

	for (int i = 0; i < 50; i++)
	{
		sum = first + second;
		printf("%d", sum);
		if (i != 49)
			printf("%s", ", ");
		temp = first;
		first = second;
		second = sum'
	}
	return (0);
}





	return (0);
}
